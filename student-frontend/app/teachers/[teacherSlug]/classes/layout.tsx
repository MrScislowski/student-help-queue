export const metadata = {
  title: "Student help queue",
  description: "by Daniel Scislowski",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
